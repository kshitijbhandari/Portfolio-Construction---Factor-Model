services:
  - type: web
    name: factor-model-optimizer
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn dash_app:server --timeout 600 --workers 1 --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
